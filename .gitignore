# Private transcripts, extracted content, research notes, and local references
/analysis/
/sources/
/data/
/references/
ponytail-reference/
ponytail-references/
*.docx
*.DOCX

# Credentials and local configuration (keep example files shareable)
.env
.env.*
!.env.example
!.env.*.example

# Python environments and generated caches
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Frontend dependencies, build output, and the generated data bundle (contains transcript text)
node_modules/
.next/
dist/
*.tsbuildinfo
/web/src/data/

# Local machine files
.DS_Store
*.log

# Local planning drafts, kept out of Git until reviewed
/MVP-BUILD-BRIEF.md
/BUILD-TEST-ROADMAP.md
